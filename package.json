{
  "name": "ichichain_bnb_test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ichichain_bnb_test",
    "create-local": "graph create --node http://localhost:8020/ ichichain_bnb_test",
    "remove-local": "graph remove --node http://localhost:8020/ ichichain_bnb_test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ichichain_bnb_test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
